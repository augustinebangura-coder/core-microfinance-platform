import React, { useEffect, useState } from 'react';\nimport { View, Text, TextInput, TouchableOpacity, StyleSheet, KeyboardAvoidingView, Platform, Alert } from 'react-native';\nimport { FORTRESS_PALETTE } from '../../../shared/constants';\n\nexport function AuthScreen() {\n  const [email, setEmail] = useState('');\n  const [password, setPassword] = useState('');\n  const [isLoading, setIsLoading] = useState(false);\n\n  const handleLogin = async () => {\n    if (!email || !password) {\n      Alert.alert('Error', 'Please fill in all fields');\n      return;\n    }\n    setIsLoading(true);\n    try {\n      // Demo authentication\n      if (email === 'officer@microfinance.com' && password === 'demo123') {\n        Alert.alert('Success', 'Logged in as Field Officer');\n      } else {\n        Alert.alert('Error', 'Invalid credentials');\n      }\n    } finally {\n      setIsLoading(false);\n    }\n  };\n\n  return (\n    <KeyboardAvoidingView behavior={Platform.OS === 'ios' ? 'padding' : 'height'} style={styles.container}>\n      <View style={styles.header}>\n        <Text style={styles.title}>C.O.R.E.</Text>\n        <Text style={styles.subtitle}>Compliance & Offline Records Engine</Text>\n      </View>\n      <View style={styles.form}>\n        <Text style={styles.label}>Email</Text>\n        <TextInput style={styles.input} placeholder=\"officer@microfinance.com\" value={email} onChangeText={setEmail} />\n        <Text style={[styles.label, { marginTop: 16 }]}>Password</Text>\n        <TextInput style={styles.input} placeholder=\"Password\" value={password} onChangeText={setPassword} secureTextEntry />\n        <TouchableOpacity style={styles.loginButton} onPress={handleLogin} disabled={isLoading}>\n          <Text style={styles.loginButtonText}>{isLoading ? 'Logging in...' : 'Login'}</Text>\n        </TouchableOpacity>\n      </View>\n    </KeyboardAvoidingView>\n  );\n}\n\nconst styles = StyleSheet.create({\n  container: { flex: 1, backgroundColor: FORTRESS_PALETTE.slateWhite },\n  header: { marginBottom: 40, alignItems: 'center', marginTop: 80 },\n  title: { fontSize: 32, fontWeight: '800', color: FORTRESS_PALETTE.primaryNavy, letterSpacing: 2 },\n  subtitle: { fontSize: 12, color: FORTRESS_PALETTE.trustBlue, marginTop: 8, letterSpacing: 1 },\n  form: { marginHorizontal: 20 },\n  label: { fontSize: 14, fontWeight: '600', color: FORTRESS_PALETTE.darkGray, marginBottom: 8 },\n  input: { borderWidth: 1, borderColor: FORTRESS_PALETTE.lightGray, borderRadius: 8, paddingHorizontal: 12, paddingVertical: 12, backgroundColor: '#FFFFFF', fontSize: 14, color: FORTRESS_PALETTE.darkGray },\n  loginButton: { marginTop: 24, backgroundColor: FORTRESS_PALETTE.trustBlue, paddingVertical: 14, borderRadius: 8, alignItems: 'center' },\n  loginButtonText: { color: '#FFFFFF', fontSize: 16, fontWeight: '700' },\n});